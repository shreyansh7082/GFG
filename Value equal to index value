class Solution {
    ArrayList<Integer> valueEqualToIndex(int arr[], int n) {
        
        ArrayList<Integer> arr2 = new ArrayList<>();
        for(int i=0; i<n;i++){
            if(arr[i] == i+1)
                arr2.add(i+1);
        }
        return arr2;
    }
}
